@using GuildsApp.Web.Models.User
@model RegisterViewModel

@{
    ViewData["Title"] = "Register";
}

<div class="auth-shell">
    <div class="auth-panel">
        <div class="auth-panel__header">
            <a href="/Feed/Index" class="auth-panel__brand">Guildle</a>
            <div class="auth-panel__subtitle">Create your account and join your first guild.</div>
        </div>

        <div class="auth-panel__body">
            <h1 class="auth-panel__title">Create account</h1>
            <p class="auth-panel__text">Start posting, joining quests, and contributing to the community.</p>

            @if (!ViewData.ModelState.IsValid)
            {
                <div class="form-alert">
                    @Html.ValidationSummary(excludePropertyErrors: true, message: "Please correct the highlighted fields.")
                </div>
            }

            <form asp-action="Register" method="post" class="auth-form">
                <div class="form-group">
                    <label asp-for="Username" class="form-label"></label>
                    <input asp-for="Username" class="form-input" autocomplete="username" />
                    <span asp-validation-for="Username" class="form-error"></span>
                </div>

                <div class="form-group">
                    <label asp-for="DisplayName" class="form-label"></label>
                    <input asp-for="DisplayName" class="form-input" autocomplete="name" />
                    <span asp-validation-for="DisplayName" class="form-error"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Password" class="form-label"></label>
                    <input asp-for="Password" type="password" class="form-input" autocomplete="new-password" />
                    <span asp-validation-for="Password" class="form-error"></span>
                </div>

                <div class="auth-panel__actions">
                    <button type="submit" class="btn btn--primary">Create account</button>
                </div>
            </form>

            <div class="auth-panel__switch">
                Already have an account?
                <a asp-action="Login">Login</a>
            </div>

            <div class="auth-panel__secondary">
                <a href="/Feed/Index">Continue as a guest</a>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
